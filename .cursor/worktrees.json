{
  "setup-worktree": [
    "bun install",
    "cp $ROOT_WORKTREE_PATH/.env .env",
    "cp $ROOT_WORKTREE_PATH/.env.local .env.local",
    "cp $ROOT_WORKTREE_PATH/.env.example .env.example 2>/dev/null || true",
    "cp $ROOT_WORKTREE_PATH/.env.template .env.template 2>/dev/null || true"
  ]
}
